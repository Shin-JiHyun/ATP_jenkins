apiVersion: v1
kind: Service
metadata:
  name: front-canary-service
  namespace: sjh
spec:
  selector:
    type: frontend
    app: user
    canary: "true"
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
